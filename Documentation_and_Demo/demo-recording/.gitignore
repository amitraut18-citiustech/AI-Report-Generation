# Regenerated by orchestrator.py / ppt_video.py — don't version intermediates.
screenshots/
audio/
# Ignore output intermediates, but keep the final .mp4 deliverables checked in
output/*
!output/*.mp4
__pycache__/
# Local Python virtual environment (recreate with: python -m venv .venv)
.venv/
# Intermediate build artifacts for the narrated deck video
ppt_audio/
ppt_slides/
ppt-seg-*.mp4
ppt-concat.txt
